Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Remove .php extension from API URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^([a-z_]+)$ $1.php [L]
</IfModule>
